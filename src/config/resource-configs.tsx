import type { ResourceConfig } from '../components/operations/ResourceScreen'
import { StatusBadge } from '../components/ui/StatusBadge'
import { clients, climateSystems, equipment, locations, technicians } from '../mocks/operational'
import type { Client, Equipment, Location, MaintenanceItem, ServiceOrder } from '../models/operational'
import { operationalService } from '../services/operational-service'
const clientName = (id: string) => clients.find((item) => item.id === id)?.name ?? '—'; const locationName = (id: string) => locations.find((item) => item.id === id)?.name ?? '—'; const equipmentName = (id: string) => equipment.find((item) => item.id === id)?.name ?? '—'; const technicianName = (id: string) => technicians.find((item) => item.id === id)?.name ?? '—'; const createId = (prefix: string) => `${prefix}-${Date.now()}`
export const clientConfig: ResourceConfig<Client> = { title: 'Clientes', description: 'Empresas atendidas e seus contatos operacionais.', singular: 'cliente', items: operationalService.clients.list(), status: (item) => item.status, searchText: (item) => `${item.name} ${item.document} ${item.contact}`, columns: [{ label: 'CLIENTE', render: (item) => <strong>{item.name}</strong> }, { label: 'CONTATO', render: (item) => item.contact }, { label: 'UNIDADES', render: (item) => item.units }, { label: 'STATUS', render: (item) => <StatusBadge status={item.status} /> }], details: (item) => [{ label: 'Documento', value: item.document }, { label: 'Contato', value: item.contact }, { label: 'Telefone', value: item.phone }, { label: 'Unidades cadastradas', value: String(item.units) }], fields: [{ name: 'name', label: 'Nome da empresa', placeholder: 'Ex.: Empresa Clima Ltda.' }, { name: 'contact', label: 'Contato responsável', placeholder: 'Nome do responsável' }, { name: 'phone', label: 'Telefone', placeholder: '(11) 99999-9999' }], create: (values) => operationalService.clients.create({ id: createId('cli'), name: values.name, contact: values.contact, phone: values.phone, document: 'Pendente', units: 0, status: 'Ativo' }) }
export const locationConfig: ResourceConfig<Location> = { title: 'Locais', description: 'Unidades e endereços que recebem atendimento.', singular: 'local', items: operationalService.locations.list(), status: (item) => item.status, searchText: (item) => `${item.name} ${item.city} ${item.address} ${clientName(item.clientId)}`, columns: [{ label: 'UNIDADE', render: (item) => <strong>{item.name}</strong> }, { label: 'CLIENTE', render: (item) => clientName(item.clientId) }, { label: 'CIDADE', render: (item) => item.city }, { label: 'STATUS', render: (item) => <StatusBadge status={item.status} /> }], details: (item) => [{ label: 'Cliente', value: clientName(item.clientId) }, { label: 'Endereço', value: item.address }, { label: 'Cidade', value: item.city }, { label: 'Responsável', value: item.manager }], fields: [{ name: 'name', label: 'Nome da unidade', placeholder: 'Ex.: Filial Norte' }, { name: 'city', label: 'Cidade', placeholder: 'São Paulo' }, { name: 'address', label: 'Endereço', placeholder: 'Rua, número' }], create: (values) => operationalService.locations.create({ id: createId('loc'), name: values.name, city: values.city, address: values.address, clientId: clients[0].id, manager: 'A definir', status: 'Ativo' }) }
export const equipmentConfig: ResourceConfig<Equipment> = { title: 'Equipamentos', description: 'Parque instalado e situação de manutenção.', singular: 'equipamento', items: operationalService.equipment.list(), status: (item) => item.status, searchText: (item) => `${item.tag} ${item.name} ${locationName(item.locationId)} ${clientName(item.clientId)}`, columns: [{ label: 'TAG', render: (item) => <strong>{item.tag}</strong> }, { label: 'EQUIPAMENTO', render: (item) => item.name }, { label: 'LOCAL', render: (item) => locationName(item.locationId) }, { label: 'STATUS', render: (item) => <StatusBadge status={item.status} /> }], details: (item) => [{ label: 'Cliente', value: clientName(item.clientId) }, { label: 'Local', value: locationName(item.locationId) }, { label: 'Sistema', value: climateSystems.find((system) => system.id === item.systemId)?.name ?? '—' }, { label: 'Última manutenção', value: item.lastMaintenance }], fields: [{ name: 'tag', label: 'Tag patrimonial', placeholder: 'ACC-005' }, { name: 'name', label: 'Nome do equipamento', placeholder: 'Ex.: Split Sala 01' }, { name: 'lastMaintenance', label: 'Última manutenção', placeholder: 'DD/MM/AAAA' }], create: (values) => operationalService.equipment.create({ id: createId('eq'), tag: values.tag, name: values.name, lastMaintenance: values.lastMaintenance, clientId: clients[0].id, locationId: locations[0].id, systemId: climateSystems[0].id, status: 'Em dia' }) }
export const orderConfig: ResourceConfig<ServiceOrder> = { title: 'Ordens de Serviço', description: 'Agenda, responsáveis e status dos atendimentos.', singular: 'ordem de serviço', items: operationalService.orders.list(), status: (item) => item.status, searchText: (item) => `${item.code} ${clientName(item.clientId)} ${locationName(item.locationId)} ${item.description}`, columns: [{ label: 'ORDEM', render: (item) => <strong>{item.code}</strong> }, { label: 'CLIENTE / LOCAL', render: (item) => <>{clientName(item.clientId)}<small>{locationName(item.locationId)}</small></> }, { label: 'AGENDAMENTO', render: (item) => item.scheduledFor }, { label: 'STATUS', render: (item) => <StatusBadge status={item.status} /> }], details: (item) => [{ label: 'Cliente', value: clientName(item.clientId) }, { label: 'Local', value: locationName(item.locationId) }, { label: 'Equipamento', value: equipmentName(item.equipmentId) }, { label: 'Técnico', value: technicianName(item.technicianId) }, { label: 'Prioridade', value: item.priority }, { label: 'Descrição', value: item.description }], fields: [{ name: 'code', label: 'Código da OS', placeholder: 'OS-1052' }, { name: 'scheduledFor', label: 'Agendamento', placeholder: '20/08 · 09:00' }, { name: 'description', label: 'Descrição', placeholder: 'Descreva o atendimento' }], create: (values) => operationalService.orders.create({ id: createId('os'), code: values.code, scheduledFor: values.scheduledFor, description: values.description, clientId: clients[0].id, locationId: locations[0].id, equipmentId: equipment[0].id, technicianId: technicians[0].id, priority: 'Média', status: 'Agendada' }) }
export const maintenanceConfig: ResourceConfig<MaintenanceItem> = { title: 'Manutenção', description: 'Plano preventivo e pendências dos equipamentos.', singular: 'item de manutenção', items: operationalService.maintenance.list(), status: (item) => item.status, searchText: (item) => `${item.title} ${equipmentName(item.equipmentId)} ${item.dueDate}`, columns: [{ label: 'ATIVIDADE', render: (item) => <strong>{item.title}</strong> }, { label: 'EQUIPAMENTO', render: (item) => equipmentName(item.equipmentId) }, { label: 'VENCIMENTO', render: (item) => item.dueDate }, { label: 'STATUS', render: (item) => <StatusBadge status={item.status} /> }], details: (item) => [{ label: 'Equipamento', value: equipmentName(item.equipmentId) }, { label: 'Frequência', value: item.frequency }, { label: 'Vencimento', value: item.dueDate }, { label: 'Responsável', value: technicianName(item.technicianId) }], fields: [{ name: 'title', label: 'Atividade', placeholder: 'Ex.: Limpeza de filtros' }, { name: 'dueDate', label: 'Vencimento', placeholder: 'DD/MM/AAAA' }, { name: 'frequency', label: 'Frequência', placeholder: 'Mensal' }], create: (values) => operationalService.maintenance.create({ id: createId('man'), title: values.title, dueDate: values.dueDate, frequency: values.frequency, equipmentId: equipment[0].id, technicianId: technicians[0].id, status: 'Em dia' }) }
