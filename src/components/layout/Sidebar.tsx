import type { NavigationItem } from '../../types/dashboard'
type SidebarProps = { items: NavigationItem[]; activeItem: string; onNavigate: (label: string) => void; isOpen: boolean; onClose: () => void }
export function Sidebar({ items, activeItem, onNavigate, isOpen, onClose }: SidebarProps) { return <><button className={`sidebar-backdrop ${isOpen ? 'is-visible' : ''}`} aria-label="Fechar menu" onClick={onClose} /><aside className={`sidebar ${isOpen ? 'is-open' : ''}`}><div className="brand" aria-label="ACC Climatização"><span className="brand__mark">A</span><span><b>ACC</b><small>CLIMATIZAÇÃO</small></span></div><nav aria-label="Navegação principal">{items.map((item) => <button className={`nav-item ${activeItem === item.label ? 'is-active' : ''}`} key={item.label} onClick={() => { onNavigate(item.label); onClose() }}><span aria-hidden="true">{item.icon}</span>{item.label}</button>)}</nav><div className="sidebar__footer">ACC Gestão Operacional <span>v1.0</span></div></aside></> }
