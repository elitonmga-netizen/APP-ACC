import type { ReactNode } from 'react'
export type TableColumn<T> = { label: string; render: (item: T) => ReactNode }
export function DataTable<T extends { id: string }>({ columns, items, onSelect }: { columns: TableColumn<T>[]; items: T[]; onSelect: (item: T) => void }) { return <div className="resource-table"><div className="resource-table__head" style={{ gridTemplateColumns: `repeat(${columns.length}, minmax(110px, 1fr))` }}>{columns.map((column) => <span key={column.label}>{column.label}</span>)}</div>{items.map((item) => <button className="resource-table__row" style={{ gridTemplateColumns: `repeat(${columns.length}, minmax(110px, 1fr))` }} key={item.id} onClick={() => onSelect(item)}>{columns.map((column) => <span key={column.label}>{column.render(item)}</span>)}</button>)}{items.length === 0 && <p className="resource-table__empty">Nenhum resultado encontrado.</p>}</div> }
